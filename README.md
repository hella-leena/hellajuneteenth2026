# HJ26 (Codex) (React + Vite)

## Run

```bash
npm install
npm run dev
```

This project recreates `https://config.figma.com` using React and CSS with matching section layout, typography scale, spacing, and dark/light treatment from the provided screenshots.
